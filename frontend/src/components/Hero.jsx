// components/Hero.jsx — Used on the Analyze page as a simple section header
// The full landing hero is in pages/LandingPage.jsx

export default function Hero() {
  return null // Replaced by AnalyzePage's inline hero
}
